# Контекст проекта «Визуал Realty»

> Последнее обновление: 25 июля 2026 года. Это основной файл передачи контекста между диалогами.

## Как продолжить работу в новом диалоге

Скопируйте в новый диалог эту фразу:

> Продолжи работу над проектом «Визуал Realty». Репозиторий: `https://github.com/prohar2f-pixel/vizual-realty`. Сначала полностью прочитай `AGENTS.md` и `PROJECT_CONTEXT.md`, затем проверь состояние Git и только после этого вноси изменения. Не показывай и не коммить секреты из `.env`.

Актуальный код находится в `origin/main`. Папка `C:\Users\Udacha\Documents\projects\vizual-realty` сейчас сильно отстаёт от удалённой ветки и содержит незакоммиченные изменения пользователя, поэтому её нельзя принудительно сбрасывать или очищать. Для новой работы безопаснее создать свежий клон или отдельный worktree от `origin/main`.

Рабочая копия последней сессии: `C:\tmp\vizual-realty-counter`. На момент обновления документации в ней сохранены посторонние незакоммиченные файлы — не добавлять их в коммиты без отдельного запроса пользователя.

## Быстрый статус

- Живой сайт: <https://nedvizhimostdoneck.ru>
- GitHub: <https://github.com/prohar2f-pixel/vizual-realty>
- Продакшен: Beget VPS, сервер **Genial Sable**
- Каталог получает объекты из CRM Topnlab.
- Последняя опубликованная функциональная версия перед этой документацией: `7cba9ec`.
- Безопасная точка отката до добавления Ольги и Виктории: `88c81a5`.
- Последняя проверка приложения: 14 файлов тестов, 47 тестов успешно; production build успешно.

## Что уже работает

- Главная страница с фотографией команды и утверждёнными надписями.
- Каталог с количеством объектов и фильтрами по комнатам, цене и реальным районам.
- Страница каждого объекта с галереей, нормализованными адресом и описанием.
- Привязка объекта к менеджеру по сотруднику, который выгрузил объект в Topnlab.
- Карточка назначенного менеджера с фото, телефоном и Telegram, если профиль известен.
- Страницы «О нас», «Команда» и «Контакты» с реальными данными.
- Карусель команды: три карточки на компьютере, одна на телефоне; стрелка сдвигает список на одного менеджера.
- В контактах восемь менеджеров, круглые фотографии с зелёной рамкой, телефон и Telegram/e-mail.
- Карта Яндекса и маршрут к офису.
- Синхронизация объектов через Topnlab API и обработчик webhook.

Основные маршруты: `/`, `/catalog`, `/object/[id]`, `/about`, `/team`, `/contacts`, `/api/lead`, `/api/webhook`.

## Стек и структура

- Next.js 16.2.9, React 19, TypeScript, Tailwind CSS.
- Prisma 7 и PostgreSQL.
- Topnlab API, собственные скрипты синхронизации.
- Nginx + PM2 на Ubuntu 22.04 в Beget VPS.

Ключевые файлы:

- `src/app/team/managers.ts` — карточки команды.
- `src/app/contacts/page.tsx` — список менеджеров, контакты офиса и карта.
- `src/lib/manager-profiles.ts` — разрешённые профили менеджеров для объектов, ключ — ID сотрудника Topnlab.
- `src/lib/topnlab/manager.ts` — определение менеджера объекта по ID, затем резервно по имени.
- `src/lib/topnlab/map.ts` — преобразование объекта Topnlab в модель сайта.
- `src/lib/topnlab/content.ts` — нормализация адресов, районов и описаний.
- `src/app/api/lead/route.ts` — отправка формы заявки в Topnlab.
- `scripts/sync.ts`, `scripts/sync-property-content.ts`, `scripts/sync-managers.ts` — ручная синхронизация.
- `prisma/schema.prisma` — модели `Property` и `Agent`.
- `docs/superpowers/specs/` и `docs/superpowers/plans/` — подробные решения по отдельным доработкам.

Важно: для этой версии Next.js перед изменением фреймворк-зависимого кода читать соответствующую документацию в `node_modules/next/dist/docs/`, как требует `AGENTS.md`.

## Менеджеры

| Менеджер | Телефон | Основной контакт | ID Topnlab |
|---|---|---|---:|
| Аянот Елена | +7 (949) 537-55-65 | `@Lena_Katana` | 296892 |
| Банитюк Юлия | +7 (949) 578-09-33 | `@Lia_banituk` | 296881 |
| Хаджинова Алина | +7 (949) 400-92-74 | `@alin_ka160` | 298110 |
| Бороха Юли | +7 (918) 295-60-93 | `@juliaborokha24` | пока нет |
| Мельник Сергей | +7 (949) 647-72-56 | `@sergeymcv` | 297093 |
| Медведева Елена | +7 (949) 715-80-77 | `@Elen_md` | пока нет |
| Ольга Кривуца | +7 (978) 059-26-69 | `olya_malina22@mail.ru` | пока нет |
| Тсаренко Виктория | +7 (963) 532-80-09 | `tsarenko.viktoria2000@mail.ru` | пока нет |

Дополнительно известен сотрудник Topnlab Антонович Виталий, ID `297092`; публичные телефон, фото и контакт для карточки объекта не утверждены.

Правило проекта: менеджера объекта нельзя выбирать вручную. Показывается только сотрудник, который выгрузил объект в базу. Если ID сотрудника отсутствует в `manager-profiles.ts`, сайт не должен подставлять случайного менеджера. Для Бороха Юли, Медведевой Елены, Ольги Кривуцы и Тсаренко Виктории нужны ID Topnlab, прежде чем привязывать к ним объекты.

## Контакты агентства

- Телефон: +7 (989) 802-84-32
- E-mail: `milmildom@mail.ru`
- Адрес: г. Донецк, ул. 50 лет СССР, 142

## Локальный запуск и проверка

На Windows использовать `npm.cmd`, потому что PowerShell может блокировать `npm.ps1`.

```powershell
npm.cmd install
npm.cmd run dev -- -p 3100
```

Для проверки режима, максимально близкого к продакшену:

```powershell
npm.cmd test
npm.cmd run lint
npm.cmd run build
npm.cmd run start -- -p 3100
```

Перед заявлением о готовности обязательно запустить тесты и build. В проекте есть старые lint-ошибки `no-explicit-any` в `src/lib/topnlab/client.ts`, `src/lib/topnlab/manager.ts`, `src/lib/topnlab/map.ts` и предупреждения в `test/map-realty-type.test.ts`; новые или изменённые файлы должны проходить lint без новых ошибок.

## Синхронизация Topnlab

Переменные `DATABASE_URL`, `TOPNLAB_KEY`, `TOPNLAB_BASE_URL` находятся в `.env`. Это действующие секреты: не печатать их в терминал, не вставлять в чат и не коммитить.

```powershell
npm.cmd run sync
npm.cmd run sync:content
npm.cmd run sync:managers
```

- `sync` получает все объекты продажи и аренды, находящиеся в фиде, и обновляет базу.
- `sync:content` повторно нормализует адреса, районы и описания существующих объектов.
- `sync:managers` повторно связывает объекты с известными менеджерами.

Объект попадёт на сайт только после включения его в фид в Topnlab. После изменений в Topnlab выполнить синхронизацию на сервере и проверить несколько объектов от разных сотрудников.

## Git и публикация

Перед коммитом:

1. Проверить `git status --short --branch` и не трогать посторонние изменения пользователя.
2. Выполнить тесты и build.
3. Получить `origin/main` и убедиться, что отправка будет fast-forward.
4. Коммитить только файлы текущей задачи.

Продакшен-проект на сервере: `/home/vizual/app`, процесс PM2: `vizual`.

Проверенная последовательность обновления через терминал Beget:

```bash
set -e; cd /home/vizual/app; git fetch origin main; git checkout origin/main -- src public package.json package-lock.json; npm run build; pm2 restart vizual; pm2 status; git rev-parse --short origin/main
```

В VNC-терминале Beget символы `&&` вводились как `77`, поэтому использовать `set -e;` и команды через `;`. Для сервера открывать терминал VPS **Genial Sable**, а не SSH виртуального хостинга. SSH виртуального хостинга оставлять выключенным.

После деплоя обязательно проверить живой сайт, особенно `/team`, `/contacts`, `/catalog` и открытую карточку объекта.

## Незавершённые задачи и решения

1. Форма заявки сейчас отправляет данные только в Topnlab. Требование заказчика на будущее: отправлять заявку в первую очередь в Topnlab и параллельно назначенному менеджеру объекта. Канал доставки менеджеру ещё не выбран; пользователь отложил решение.
2. Получить ID Topnlab для Бороха Юли, Медведевой Елены, Ольги Кривуцы и Тсаренко Виктории.
3. Заменить общие тексты в карточках команды на реальные опыт, достижения, число сделок и проданных объектов после получения данных от заказчика.
4. При следующих изменениях каталога проверять, что фильтр «Район» содержит районы, а не города, и что адрес/описание совпадают с Topnlab по смыслу и переносам строк.

## Утверждённые принципы

- Сначала показывать локальный результат пользователю, затем публиковать, если пользователь не попросил сразу обновить живой сайт.
- Качество фотографий не снижать заметной агрессивной компрессией.
- Аватары менеджеров — в зелёной рамке.
- Не очищать и не сбрасывать dirty worktree без прямого разрешения.
- Любое действие можно откатить Git-коммитом; перед рискованными изменениями записывать текущий hash `main`.

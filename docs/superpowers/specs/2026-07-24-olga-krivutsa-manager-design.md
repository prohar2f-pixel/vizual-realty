# Добавление Ольги Кривуцы и Виктории Тсаренко в разделы «Команда» и «Контакты»

## Цель

Добавить Ольгу Кривуцу и Викторию Тсаренко как седьмого и восьмого менеджеров агентства, сохранив существующий внешний вид карточек, карусели и контактного блока.

## Данные менеджера

- Имя: Ольга Кривуца
- Телефон: +7 (978) 059-26-69
- Ссылка телефона: `tel:+79780592669`
- E-mail: `olya_malina22@mail.ru`
- Фотография: `D2BA67CE-020E-437F-96B0-BF9D9B2BE6FE.jpg`

Telegram для Ольги не указан. Вместо Telegram используется ссылка `mailto:olya_malina22@mail.ru`.

- Имя: Тсаренко Виктория
- Телефон: +7 (963) 532-80-09
- Ссылка телефона: `tel:+79635328009`
- E-mail: `tsarenko.viktoria2000@mail.ru`
- Фотография: `IMG_3888.JPG`

Telegram для Виктории не указан. Вместо Telegram используется ссылка `mailto:tsarenko.viktoria2000@mail.ru`.

## Раздел «Команда»

- Ольга и Виктория добавляются седьмой и восьмой карточками в существующую карусель.
- На компьютере по-прежнему одновременно видны три карточки.
- Стрелки по-прежнему смещают карусель на одного менеджера.
- На мобильном устройстве отображается одна карточка.
- Фотография оформляется в существующей зелёной рамке карточки и кадрируется без искажения пропорций.
- Телефон остаётся кликабельным.
- У Ольги и Виктории кнопка связи называется «Написать на e-mail» и открывает почтовое приложение.
- У остальных менеджеров кнопки Telegram и ссылки остаются без изменений.

## Раздел «Контакты»

- Последняя карточка-заглушка заменяется реальной карточкой Ольги, после неё добавляется карточка Виктории.
- Аватар отображается круглым, с зелёной рамкой, как у остальных менеджеров.
- Телефон остаётся кликабельным.
- Кнопка Ольги называется «Написать на e-mail» и ведёт на `mailto:olya_malina22@mail.ru`.
- Кнопка Виктории называется «Написать на e-mail» и ведёт на `mailto:tsarenko.viktoria2000@mail.ru`.
- Сетка существующего блока контактов не меняется. Высота списка адаптируется для восьми реальных менеджеров без наложений и обрезания содержимого.

## Модель данных интерфейса

Карточки получают общий способ связи:

- адрес ссылки;
- подпись кнопки;
- доступная подпись для экранных дикторов;
- признак открытия в новой вкладке только для внешних ссылок.

Это исключает пустые Telegram-ссылки и позволяет использовать e-mail без отдельной копии карточки.

## Изображения

Исходные фотографии преобразуются в оптимизированные WebP-версии для карточек «Команды» и круглых аватаров в «Контактах». Исходники не изменяются. Качество подбирается так, чтобы лица и детали одежды не выглядели размытыми.

## Проверка

- Тест подтверждает корректные e-mail-ссылки и подписи кнопок Ольги и Виктории.
- Тест подтверждает, что Telegram-ссылки остальных менеджеров не изменились.
- Проверяются TypeScript, линтер, тесты и production-сборка.
- После запуска локального сайта визуально проверяются «Команда» и «Контакты» на компьютерной и мобильной ширине.

## Вне текущей задачи

Ольга и Виктория не привязываются к объектам Topnlab, пока не будут предоставлены их идентификаторы сотрудников и подтверждено наличие объектов в фиде.
